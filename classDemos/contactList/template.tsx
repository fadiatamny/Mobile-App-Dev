import React from 'react';
import {
    StyleSheet,
    Text,
    View,
} from 'react-native';

const gridItem = (): any => {
    return (
        <View>
            <Text> test</Text>
        </View>
    );
}

const styles = StyleSheet.create({
});

export default gridItem;
